import AccountInfo from "@/components/AccountInfo";

export default function Account() {

    return (
        <section className="h-full max-w-4xl w-full p-4 mx-auto flex">
            <AccountInfo />
        </section>
    );
}